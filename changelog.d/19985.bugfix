Fix a bug where the presence federation queue would accumulate updates and advance the `presence_federation` replication stream indefinitely when outbound federation was disabled, causing spurious `wait_for_stream_position` timeouts on worker deployments. Contributed by @tedroden
