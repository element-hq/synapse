Fix the `oidc_session_no_samesite` cookie to also be have the `Secure` attribute like paired `oidc_session` so the only difference between them is the configuration of the `SameSite` attribute as described in the comments / cookie names.